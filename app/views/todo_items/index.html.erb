<p id="notice"><%= notice %></p>

<div class="jumbotron">


<h1 class="title_todo ">Todo Items</h1>

<div class="title-im"></div>

<table class="table table-hover">
  <thead class="thead-dark">
    <tr>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @todo_items.each do |todo_item| %>
      <tr>
        <td><%= todo_item.description %></td>
        <td><%= link_to 'Show', todo_item, class: "btn btn-outline-primary" %></td>
        <td><%= link_to 'Edit', edit_todo_item_path(todo_item), class:"btn btn-outline-success" %></td>
        <td><%= link_to '💩', todo_item, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-outline-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</div>

<%= link_to 'New Todo Item', new_todo_item_path, class:"btn btn-lg btn-primary"  %>
